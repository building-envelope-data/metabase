query AllData(
  $where: DataPropositionInput! # $timestamp: DateTime # $locale: Locale # $first: NonNegativeInt
) # $after: Cursor
# $last: NonNegativeInt
# $before: Cursor
{
  allData(
    where: $where # timestamp: $timestamp # locale: $locale # first: $first
  ) # after: $after
  # last: $last
  # before: $before
  {
    # edges {
    #   cursor
    #   node {
    #     ...DataFields
    #   }
    # }
    nodes {
      ...DataFields
    }
    # totalCount
    # pageInfo {
    #   ...PageInfoFields
    # }
    # timestamp
  }
}
