query Component(
    $id: Uuid!
) {
    component(id: $id) {
        id
        uuid
        name
        abbreviation
        description
        availability {
            from
            to
        }
        categories
    }
}