query Components($order: [ComponentSortInput!]) {
  components(order: $order) {
    edges {
      node {
        id
        uuid
        name
        abbreviation
        description
        availability {
          from
          to
        }
        categories
      }
    }
  }
}

