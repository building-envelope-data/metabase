mutation changeUserEmail(
    $newEmail: String!
) {
  changeUserEmail(input: {
    newEmail: $newEmail
  }) {
    user {
      id
      contact {
        emailAddress
      }
    }
    errors {
      code
      message
      path
    }
  }
}