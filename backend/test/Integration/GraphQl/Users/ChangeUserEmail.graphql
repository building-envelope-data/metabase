mutation changeUserEmail(
    $newEmail: EmailAddress!
) {
  changeUserEmail(input: {
    newEmail: $newEmail
  }) {
    user {
      id
      email
    }
    errors {
      code
      message
      path
    }
  }
}