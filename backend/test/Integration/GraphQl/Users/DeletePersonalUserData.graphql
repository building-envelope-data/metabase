mutation DeletePersonalUserData(
    $password: String,
) {
  deletePersonalUserData(input: {
    password: $password
  }) {
    user {
      id
      contact {
        emailAddress
      }
    }
    errors {
      code
      message
      path
    }
  }
}