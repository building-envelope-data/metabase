mutation loginUser($email: String!, $password: String!) {
  loginUser(
    input: { email: $email, password: $password }
  ) {
    requiresTwoFactor
    user {
      id
      contact {
        emailAddress
      }
    }
    errors {
      code
      message
      path
    }
  }
}
