mutation verifyInstitution($institutionId: Uuid!) {
  verifyInstitution(input: { institutionId: $institutionId }) {
    institution {
      uuid
    }
    errors {
      code
      message
      path
    }
  }
}
