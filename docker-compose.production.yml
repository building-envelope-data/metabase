# Keep setup in sync with the one in ./docker-compose.yml
name: "${NAME}"

services:
  database:
    # Keep version in sync with the one in ./backend/src/Startup.cs set by calling `SetPostgresVersion`
    image: "postgres:13.23-trixie"
    volumes:
      - data:/var/lib/postgresql/data
      - ./secrets:/run/secrets:ro
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD_FILE: "/run/secrets/postgres_password"
      POSTGRES_DB: "postgres"
      PGPASSFILE: "/run/secrets/postgres_passwords"
    # For possible command-line arguments see
    # https://www.postgresql.org/docs/current/app-postgres.html
    # and for possible configuration parameters like `max_connections` see
    # https://www.postgresql.org/docs/current/runtime-config.html
    command: [ "-c", "max_connections=100" ]
    restart: "always"
    healthcheck:
      # pg_isready: https://www.postgresql.org/docs/13/app-pg-isready.html
      test: [ "CMD-SHELL", "pg_isready --username=postgres" ]
      interval: "5s"
      timeout: "5s"
      retries: "5"

  email:
    image: "rnwood/smtp4dev:v3.10"
    volumes:
      - emails:/smtp4dev
    environment:
      ServerOptions__BasePath: "/email"
      ServerOptions__HostName: "${HOST:?required}"
      ServerOptions__Database: "database.db"
      ServerOptions__NumberOfMessagesToKeep: "100"
      ServerOptions__NumberOfSessionsToKeep: "100"
      ServerOptions__TlsMode: "None"
      ServerOptions__TlsCertificate: ""
      RelayOptions__SmtpServer: "${RELAY_SMTP_HOST?required}"
      RelayOptions__SmtpPort: "${RELAY_SMTP_PORT:?required}"
      RelayOptions__AllowedEmailsString: "${RELAY_ALLOWED_EMAILS?required}"
    restart: "always"
    # For all configuration options see
    # https://github.com/rnwood/smtp4dev/blob/master/docker-compose.yml
    healthcheck:
      test: [ "CMD-SHELL", "curl --silent --fail --output /dev/null http://email:80 || exit 1" ]
      interval: "30s"
      timeout: "30s"
      retries: "3"

  backend:
    build:
      context: "./backend"
      dockerfile: "./Dockerfile-production"
    depends_on:
      database:
        condition: "service_healthy"
      email:
        condition: "service_healthy"
    volumes:
      - backend_logs:/app/logs
      - ./secrets:/run/secrets:ro
      # Create volume for GnuPG
      - gnupg:/root/.gnupg
    environment:
      ASPNETCORE_URLS: "http://0.0.0.0:8080"
      ASPNETCORE_ENVIRONMENT: "production"
      XBASE_Host: "https://${HOST:?required}"
      XBASE_BootstrapUserPassword: "${BOOTSTRAP_USER_PASSWORD:?required}"
      XBASE_OpenIdConnectClientSecret: "${OPEN_ID_CONNECT_CLIENT_SECRET:?required}"
      XBASE_JsonWebToken__EncryptionCertificatePassword: "${JSON_WEB_TOKEN_ENCRYPTION_CERTIFICATE_PASSWORD:?required}"
      XBASE_JsonWebToken__SigningCertificatePassword: "${JSON_WEB_TOKEN_SIGNING_CERTIFICATE_PASSWORD:?required}"
      XBASE_Email__SmtpHost: "${SMTP_HOST:?required}"
      XBASE_Email__SmtpPort: "${SMTP_PORT:?required}"
      XBASE_TestlabSolarFacadesHost: "https://${TESTLAB_SOLAR_FACADES_HOST:?required}"
      XBASE_TestlabSolarFacadesOpenIdConnectClientSecret: "${TESTLAB_SOLAR_FACADES_OPEN_ID_CONNECT_CLIENT_SECRET:?required}"
      XBASE_IgsdbApiToken: "${IGSDB_API_TOKEN:?required}"
      XBASE_IgsdbOpenIdConnectClientSecret: "${IGSDB_OPEN_ID_CONNECT_CLIENT_SECRET:?required}"
      # The value `/dev/pts/0` is the output of running `tty` within `shellb`
      # https://www.gnupg.org/documentation/manuals/gnupg/Invoking-GPG_002dAGENT.html
      GPG_TTY: "/dev/pts/0"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # https://docs.microsoft.com/en-us/aspnet/core/fundamentals/configuration/?view=aspnetcore-5.0#command-line
    # command: ["dotnet", "App.dll", "/Serilog:MinimumLevel:Default=Debug", "/Serilog:MinimumLevel:Microsoft=Debug", "/Serilog:MinimumLevel:Microsoft.Hosting.Lifetime=Debug", "/Serilog:MinimumLevel:Override:Microsoft=Debug", "/Serilog:MinimumLevel:Override:Microsoft.AspNetCore=Debug", "/Serilog:MinimumLevel:Override:System=Debug"]
    command: [ "dotnet", "/app/App.dll" ]
    restart: "always"
    healthcheck:
      # https://docs.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks?view=aspnetcore-5.0#docker-example
      test: [ "CMD-SHELL", "curl --silent --fail --output /dev/null http://backend:8080/health || exit 1" ]
      interval: "30s"
      timeout: "30s"
      retries: "3"

  frontend:
    build:
      context: "./frontend"
      dockerfile: "./Dockerfile-production"
    depends_on:
      backend:
        condition: "service_healthy"
    environment:
      NODE_ENV: "production"
    command: [ "yarn", "start" ]
    restart: "always"
    healthcheck:
      test: [ "CMD-SHELL", "wget --quiet --output-document /dev/null http://frontend:8080 || exit 1" ]
      interval: "30s"
      timeout: "30s"
      retries: "3"

  nginx:
    image: "nginx:1.29-trixie-otel"
    depends_on:
      backend:
        condition: "service_healthy"
      email:
        condition: "service_healthy"
      frontend:
        condition: "service_healthy"
    volumes:
      - ./nginx/templates/production:/etc/nginx/templates:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/html:/etc/nginx/html:ro
    ports:
      - "0.0.0.0:${HTTP_PORT:?required}:80"
    command: [ "nginx", "-g", "daemon off;" ]
    restart: "always"
    healthcheck:
      test: [ "CMD-SHELL", "service nginx status || exit 1" ]
      interval: "30s"
      timeout: "30s"
      retries: "3"

volumes:
  data:
  emails:
  gnupg:
  backend_logs:
