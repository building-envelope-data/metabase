mutation confirmUserEmail(
  $confirmationCode: String!
  ) {
  confirmUserEmail(input: {
    email: "unknown.user@unknown-provider.org",
    confirmationCode: $confirmationCode
  }) {
    user {
      id
      email
    }
    errors {
      code
      message
      path
    }
  }
}
