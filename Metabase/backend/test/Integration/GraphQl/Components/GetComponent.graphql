query component(
    $uuid: Uuid!
) {
    component(uuid: $uuid) {
        id
        uuid
        name
        abbreviation
        description
        availability {
            from
            to
        }
        categories
    }
}